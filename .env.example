# --- Provider keys (SECRET — never commit the real .env) ---
ANTHROPIC_API_KEY=sk-ant-...
# Optional: only needed once routing sends traffic to OpenAI models.
OPENAI_API_KEY=sk-...
# Optional: powers the Phase-2.5 LLM classifier tie-break (Groq-hosted
# llama-3.1-8b-instant). Get one at https://console.groq.com -> API Keys.
# Empty => tie-break disabled, heuristics-only routing.
GROQ_API_KEY=gsk_...

# --- LLM classifier tie-break (Phase 2.5) ---
# The heuristic classifier runs on every request; the LLM is consulted ONLY
# when heuristic confidence < threshold, so cost/latency stay near-zero.
LLM_TIEBREAK_ENABLED=true
LLM_TIEBREAK_THRESHOLD=0.6        # consult LLM when heuristic confidence is below this
LLM_CLASSIFIER_MODEL=llama-3.1-8b-instant
LLM_CLASSIFIER_TIMEOUT_MS=700     # abort + fall back to heuristic if slower

# --- Server ---
PORT=3000
HOST=0.0.0.0

# --- Gateway auth ---
# Comma-separated plaintext client keys (dev). Empty AND no hashes => auth off.
GATEWAY_API_KEYS=dev-key-123
# Production: comma-separated sha256 hex hashes of accepted keys (raw never stored).
#   printf '%s' "your-key" | sha256sum
GATEWAY_API_KEY_HASHES=

# --- Hardening (Phase 5). 0 disables a given limit. ---
RATE_LIMIT_PER_MIN=60
DAILY_BUDGET_USD=0
MAX_REQUEST_COST_USD=0
MAX_PROMPT_TOKENS=150000
JAILBREAK_BLOCK=true
PII_MODE=flag           # flag | block

# --- Defaults ---
DEFAULT_MODEL=claude-haiku-4-5
DEFAULT_MAX_TOKENS=1024
DEFAULT_TEMPERATURE=0.7

# --- Observability (Phase 4) ---
# Model used as the "naive baseline" for the savings comparison in /v1/usage.
BASELINE_MODEL=claude-sonnet-4-6
# Optional append-only JSONL log file. Empty = in-memory only.
USAGE_LOG_FILE=
