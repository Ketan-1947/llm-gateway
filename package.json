{
  "name": "llm-gateway",
  "version": "0.1.0",
  "description": "LLM middleware gateway — Phase 1 skeleton: Claude pass-through with cost & latency metadata.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch --env-file=.env src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file=.env dist/src/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:routing": "tsx test/routing.test.ts",
    "test:optimizer": "tsx test/optimizer.test.ts",
    "test:usage": "tsx test/usage.test.ts",
    "test:hardening": "tsx test/hardening.test.ts",
    "test": "tsx test/routing.test.ts && tsx test/optimizer.test.ts && tsx test/usage.test.ts && tsx test/hardening.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "openai": "^4.104.0",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
